pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo 'Jenkins pipeline is alive'
                sh 'pwd'
                sh 'whoami'
                sh 'date'
            }
        }
    }
}
